'''
no toque este archivo apps.py, creo que se modifico
al momento de crear la app main
'''

from django.apps import AppConfig

class MainConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'main'
